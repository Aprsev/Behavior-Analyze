# Install a CUDA-enabled PyTorch build suitable for the execution host first.
# Then install this file. Model Zoo weights are downloaded on first inference.
deeplabcut[modelzoo]>=3.0.0,<4
opencv-python>=4.8
numpy>=1.24
pandas>=2.0,<3
tables>=3.9
